<Window x:Class="DashLook.Windows.StartupBannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        Opacity="0"
        Loaded="Window_Loaded">
    <Border Background="{DynamicResource WindowBackground}"
            BorderBrush="{DynamicResource BorderLight}"
            BorderThickness="1"
            CornerRadius="18"
            Padding="18,16"
            Effect="{DynamicResource WindowShadow}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Width="44"
                    Height="44"
                    CornerRadius="14"
                    Background="{DynamicResource AccentColor}">
                <Image Source="/Resources/dashlook_tray.ico"
                       Width="22"
                       Height="22"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1" Margin="14,0,18,0">
                <TextBlock x:Name="TitleText"
                           Text="Starting DashLook"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock x:Name="SubtitleText"
                           Margin="0,4,0,0"
                           Text="RotexIT  |  ROTEXIT.COM"
                           FontSize="12"
                           Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>

            <Border Grid.Column="2"
                    VerticalAlignment="Center"
                    Background="{DynamicResource HoverBackground}"
                    CornerRadius="999"
                    Padding="10,4">
                <TextBlock x:Name="StateText"
                           Text="Booting"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource AccentColor}"/>
            </Border>
        </Grid>
    </Border>
</Window>

