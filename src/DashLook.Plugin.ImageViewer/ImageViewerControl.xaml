<UserControl x:Class="DashLook.Plugin.ImageViewer.ImageViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Rectangle>
            <Rectangle.Fill>
                <DrawingBrush TileMode="Tile"
                              Viewport="0,0,16,16"
                              ViewportUnits="Absolute">
                    <DrawingBrush.Drawing>
                        <DrawingGroup>
                            <GeometryDrawing Brush="{DynamicResource SurfaceBackground}">
                                <GeometryDrawing.Geometry>
                                    <RectangleGeometry Rect="0,0,16,16"/>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                            <GeometryDrawing Brush="{DynamicResource HoverBackground}">
                                <GeometryDrawing.Geometry>
                                    <GeometryGroup>
                                        <RectangleGeometry Rect="0,0,8,8"/>
                                        <RectangleGeometry Rect="8,8,8,8"/>
                                    </GeometryGroup>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                        </DrawingGroup>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Rectangle.Fill>
        </Rectangle>

        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      x:Name="Scroller"
                      Background="Transparent">
            <Image x:Name="MainImage"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   MouseWheel="Image_MouseWheel"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="16">
                <Image.LayoutTransform>
                    <ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1"/>
                </Image.LayoutTransform>
            </Image>
        </ScrollViewer>

        <Border x:Name="ZoomIndicator"
                Background="{DynamicResource WindowBackground}"
                BorderBrush="{DynamicResource BorderLight}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8,4"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="10"
                Opacity="0">
            <TextBlock x:Name="ZoomText"
                       Text="100%"
                       Foreground="{DynamicResource TextPrimary}"
                       FontSize="11"
                       FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>
