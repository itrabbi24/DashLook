<?xml version="1.0" encoding="UTF-8"?>
<!--
  DashLook WiX v4 Installer
  Requires: WiX Toolset v4+
  Build:    dotnet build installer\DashLook.Installer.wixproj -c Release
  Developer: ARG RABBI — https://itrabbi24.github.io/
-->
<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs"
     xmlns:ui="http://wixtoolset.org/schemas/v4/wxs/ui">

  <Package Name="DashLook"
           Manufacturer="ARG RABBI"
           Version="1.0.0.0"
           UpgradeCode="DCA7F3B1-4E2A-4F3D-9B1C-8E2D7A4B5C6F"
           Language="1033"
           Codepage="1252"
           InstallerVersion="500"
           Compressed="yes">

    <SummaryInformation Description="DashLook — Instant file preview for Windows. Developed by ARG RABBI (itrabbi24.github.io)"/>

    <MajorUpgrade DowngradeErrorMessage="A newer version of DashLook is already installed." />

    <MediaTemplate EmbedCab="yes"/>

    <!-- ── UI ────────────────────────────────────────────────────────────── -->
    <ui:WixUI Id="WixUI_InstallDir"/>
    <Property Id="WIXUI_INSTALLDIR" Value="INSTALLFOLDER"/>

    <!-- Finish dialog: show developer info and optional "open website" checkbox -->
    <Property Id="WIXUI_EXITDIALOGOPTIONALTEXT"
              Value="DashLook was installed successfully!&#10;&#10;Developed by ARG RABBI — itrabbi24.github.io&#10;Press Space in Explorer to preview any file." />

    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOXTEXT"
              Value="Visit developer website (itrabbi24.github.io)" />

    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOX" Value="1"/>

    <!-- ── Launch app after install ──────────────────────────────────────── -->
    <Property Id="WixShellExecTarget" Value="[INSTALLFOLDER]DashLook.exe"/>
    <CustomAction Id="LaunchAppOnExit"
                  BinaryRef="Wix4UtilCA_X64"
                  DllEntry="WixShellExec"
                  Impersonate="yes"
                  Return="ignore"/>

    <!-- Open developer profile if checkbox ticked on finish -->
    <Property Id="DEVELOPER_PROFILE_URL" Value="https://itrabbi24.github.io/"/>
    <CustomAction Id="OpenDevProfile"
                  BinaryRef="Wix4UtilCA_X64"
                  DllEntry="WixShellExec"
                  Impersonate="yes"
                  Return="ignore">
      <ProgId Id="OpenDevProfileBrowser">
        <Verb Id="open" Target="[DEVELOPER_PROFILE_URL]"/>
      </ProgId>
    </CustomAction>

    <InstallExecuteSequence>
      <Custom Action="LaunchAppOnExit" After="InstallFinalize">
        NOT Installed AND NOT REMOVE
      </Custom>
    </InstallExecuteSequence>

    <!-- ── Install directory ──────────────────────────────────────────────── -->
    <StandardDirectory Id="ProgramFiles6432Folder">
      <Directory Id="INSTALLFOLDER" Name="DashLook">
        <Directory Id="PLUGINSFOLDER" Name="Plugins"/>
      </Directory>
    </StandardDirectory>

    <StandardDirectory Id="ProgramMenuFolder">
      <Directory Id="ApplicationProgramsFolder" Name="DashLook"/>
    </StandardDirectory>

    <StandardDirectory Id="DesktopFolder"/>

    <!-- ── Components ────────────────────────────────────────────────────── -->
    <ComponentGroup Id="MainExecutable" Directory="INSTALLFOLDER">
      <Component Id="DashLookExe" Guid="A1B2C3D4-1111-1111-1111-111111111111">
        <File Id="DashLookExeFile"
              Source="$(var.DashLook.TargetPath)"
              KeyPath="yes"/>
        <!-- Auto-start DashLook when Windows logs in -->
        <RegistryValue Root="HKCU"
                       Key="Software\Microsoft\Windows\CurrentVersion\Run"
                       Name="DashLook"
                       Value="[INSTALLFOLDER]DashLook.exe"
                       Type="string"/>
        <!-- Store developer info in registry for About dialog -->
        <RegistryValue Root="HKCU"
                       Key="Software\DashLook"
                       Name="Developer"
                       Value="ARG RABBI — https://itrabbi24.github.io/"
                       Type="string"/>
        <RegistryValue Root="HKCU"
                       Key="Software\DashLook"
                       Name="Version"
                       Value="1.0.0"
                       Type="string"/>
      </Component>

      <Component Id="RuntimeFiles" Guid="B2C3D4E5-2222-2222-2222-222222222222">
        <File Source="$(var.DashLook.TargetDir)DashLook.Common.dll" KeyPath="yes"/>
      </Component>
    </ComponentGroup>

    <!-- Plugin components -->
    <ComponentGroup Id="Plugins" Directory="PLUGINSFOLDER"/>

    <!-- Start menu shortcuts -->
    <ComponentGroup Id="Shortcuts" Directory="ApplicationProgramsFolder">
      <Component Id="StartMenuShortcut" Guid="C3D4E5F6-3333-3333-3333-333333333333">
        <Shortcut Id="StartMenuShortcutDashLook"
                  Name="DashLook"
                  Description="Instant file preview — by ARG RABBI"
                  Target="[INSTALLFOLDER]DashLook.exe"
                  WorkingDirectory="INSTALLFOLDER"/>
        <Shortcut Id="StartMenuShortcutWebsite"
                  Name="DashLook — Developer Website"
                  Description="Visit ARG RABBI's developer profile"
                  Target="https://itrabbi24.github.io/"/>
        <RemoveFolder Id="RemoveStartMenu" On="uninstall"/>
        <RegistryValue Root="HKCU"
                       Key="Software\DashLook"
                       Name="Installed"
                       Value="1"
                       Type="integer"
                       KeyPath="yes"/>
      </Component>
    </ComponentGroup>

    <!-- Desktop shortcut -->
    <ComponentGroup Id="DesktopShortcuts" Directory="DesktopFolder">
      <Component Id="DesktopShortcut" Guid="D4E5F6A7-4444-4444-4444-444444444444">
        <Shortcut Id="DesktopShortcutDashLook"
                  Name="DashLook"
                  Description="Instant file preview for Windows"
                  Target="[INSTALLFOLDER]DashLook.exe"
                  WorkingDirectory="INSTALLFOLDER"/>
        <RegistryValue Root="HKCU"
                       Key="Software\DashLook"
                       Name="DesktopShortcut"
                       Value="1"
                       Type="integer"
                       KeyPath="yes"/>
      </Component>
    </ComponentGroup>

    <!-- Feature -->
    <Feature Id="Complete" Title="DashLook" Level="1">
      <ComponentGroupRef Id="MainExecutable"/>
      <ComponentGroupRef Id="Plugins"/>
      <ComponentGroupRef Id="Shortcuts"/>
      <ComponentGroupRef Id="DesktopShortcuts"/>
    </Feature>

  </Package>
</Wix>
